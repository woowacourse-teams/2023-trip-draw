CREATE TABLE `area`
(
    `created_at` TIMESTAMP,
    `area_id`  BIGINT PRIMARY KEY AUTO_INCREMENT,
    `updated_at` TIMESTAMP,
    `sido`   VARCHAR(255),
    `sigungu`   VARCHAR(255),
    `umd`   VARCHAR(255)
);
